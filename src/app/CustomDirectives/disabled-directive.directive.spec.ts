import { DisabledDirectiveDirective } from './disabled-directive.directive';

describe('DisabledDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
